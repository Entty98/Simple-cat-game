# Simple Cat Game — Information

👾 A very simple two-dimensional game written in C# and developed using Visual Studio Code.

## Overview

In this game the player controls a cat on a small map. The goal is to catch falling onions before they are lost. The player receives a positive point for each onion caught and a negative point for each onion lost.

## Gameplay

- Player: a cat that the user moves around the map.
- Objects: onions appear and the cat must catch them.
- Scoring: +1 for each onion caught, -1 for each onion lost.

## Project structure (high level)

- Source code: C# files containing the game logic (e.g., Program.cs, Game.cs, Player.cs).
- Assets/: images and sounds used by the game (sprites, background, etc.).
- Information.md / README.md: documentation and instructions.

## How to run (basic)

1. Open the project folder in Visual Studio Code or another C#-capable IDE.
2. Make sure you have the .NET SDK or a compatible runtime installed if the project targets .NET. (If the project uses a different runtime or engine, follow the specific instructions in README.md.)
3. Build and run the project from the IDE or using the appropriate command (for .NET projects: `dotnet run` in the project directory).

Note: This project was created as a practice exercise and may use simple project settings suitable for learning. If you run into build/runtime issues, check the project files or open an issue describing the problem.

## License & Intent

📌 Note: This project is purely for practice and learning. It is not intended to infringe on anyone else's work or rights.

## Contribution & Contact

If you want to contribute, feel free to open an issue or a pull request. Any small improvements, bug fixes, or documentation updates are welcome.

---

# بازی ساده گربه — اطلاعات (فارسی)

👾 یک بازی دوبعدی بسیار ساده نوشته‌شده با زبان #C و توسعه‌یافته در Visual Studio Code.

## خلاصه

در این بازی بازیکن یک گربه را کنترل می‌کند که روی یک نقشه حرکت می‌کند. هدف گرفتن پیازها (onion) قبل از گم‌شدن است. به ازای هر پیاز گرفته‌شده امتیاز مثبت و به ازای هر پیاز گمشده امتیاز منفی دریافت می‌شود.

## نحوه بازی

- بازیکن: یک گربه که کاربر آن را حرکت می‌دهد.
- اشیاء: پیازها که ظاهر می‌شوند و باید گرفته شوند.
- امتیازدهی: +1 برای هر پیاز گرفته‌شده، -1 برای هر پیاز گم‌شده.

## ساختار پروژه (خلاصه)

- کد منبع: فایل‌های C# شامل منطق بازی (مثل Program.cs، Game.cs، Player.cs).
- Assets/: تصاویر و صداهای استفاده‌شده در بازی.
- Information.md / README.md: مستندات و دستورالعمل‌ها.

## چگونه اجرا کنم (کوتاه)

1. پوشه پروژه را در Visual Studio Code یا هر محیط توسعه‌ی دیگری که از C# پشتیبانی می‌کند باز کنید.
2. مطمئن شوید که SDK یا runtime مورد نیاز نصب است (برای پروژه‌های .NET از .NET SDK استفاده کنید). اگر پروژه از ابزار یا پلتفرم دیگری استفاده می‌کند، از دستورالعمل‌های مربوطه در README.md پیروی کنید.
3. پروژه را از طریق IDE بسازید و اجرا کنید یا از خط فرمان مناسب استفاده کنید (برای پروژه‌های .NET: `dotnet run`).

نکته: این پروژه برای تمرین ساخته شده و ممکن است تنظیمات ساده‌ای داشته باشد؛ اگر مشکلی در ساخت یا اجرا داشتید، لطفاً یک issue باز کنید و مشکل را توصیف کنید.

## مشارکت

برای مشارکت می‌توانید issue یا pull request باز کنید. هر بهبود کوچک یا اصلاح مستندات خوش‌آمد است.
